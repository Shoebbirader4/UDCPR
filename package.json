{
  "name": "udcpr-master",
  "version": "1.0.0",
  "description": "AI-powered UDCPR 2020 compliance superapp for Maharashtra",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["udcpr", "architecture", "compliance", "maharashtra"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
